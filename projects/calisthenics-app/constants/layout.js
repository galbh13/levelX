// Shared layout tokens.
// CARD_W / CARD_H — the fixed width (frame max-width) and inner height of the
// framed player workout cards (Weekly Plan, Elite Workouts, Create Workout,
// My Workouts) so every card frame is the same size regardless of its data.
// Tune here once to resize all four together.
export const CARD_W = 1200;
export const CARD_H = 880;
